{
  "name": "ctfbin-server",
  "version": "1.0.0",
  "description": "a backend server of the ctfbin platform",
  "main": "server.js",
  "scripts": {
    "debug": "nodemon --inspect=0.0.0.0:9229 server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.12"
  },
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
